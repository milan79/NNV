@model IEnumerable<NNV.Models.VrsteDokumenata>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VrstaDokumenta)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VrstaDokumenta)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DokumentID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DokumentID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DokumentID })
        </td>
    </tr>
}

</table>
